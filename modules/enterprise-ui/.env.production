# Production build (containerized): talk to the real adapter over same-origin.
# nginx proxies /api -> the adapter container, so an empty VITE_API_URL means
# "call the same origin the UI was served from".
VITE_USE_MOCK=false
VITE_API_URL=
